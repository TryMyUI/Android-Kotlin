package com.mahesch.trymyui.helpers;

import com.google.gson.Gson;
import com.seattleapplab.trymyui.models.Tests;

import java.util.ArrayList;

public class Extra {
    public static void main(String args){
        for(int i=0;i<10;i++){
           // ArrayList<Tests.SusQuestions> susList = new Gson().fromJson(susQuestionString,ArrayList<Tests.SusQuestions>())
        }
    }

      /* val factory = RatingViewModelFactory(application)

        var ratingViewModel = ViewModelProvider(this,factory).get(RatingViewModel::class.java)

        ratingViewModel.submitRateForWorker(sharedPrefHelper?.getToken(),5)?.observe(this,object : Observer<CommonModel>{
            override fun onChanged(commonModel: CommonModel?) {

                if(commonModel == null){

                }
                else
                {
                    if(commonModel.error == null){

                    }
                    else
                    {

                    }
                }

            }

        })

        ratingViewModel.submitRateForGuest(sharedPrefHelper?.getEmailId(),5)?.observe(this,object : Observer<CommonModel>{
            override fun onChanged(t: CommonModel?) {

            }

        })*/





}
